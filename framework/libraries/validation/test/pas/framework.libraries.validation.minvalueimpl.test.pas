unit Framework.Libraries.Validation.MinValueImpl.Test;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit
  being tested.

}

interface

uses
  TestFramework,
  Framework.Libraries.Validation.Validate,
  Framework.Libraries.Validation.MinValueImpl,
  Framework.Libraries.Validation.ValidateCustomAttributeImpl,
  System.Rtti;

type
  // Test methods for class TMinValue

  TestTMinValue = class(TTestCase)
  strict private
    FMinValue: TMinValueImpl;
  public
    const
    COLUMN_NAME: String = 'MyProp';
    MIN_VALUE: Integer = 10;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure GetErrorMessage;
    procedure IsValidString;
    procedure IsValidCurrencyLittle;
    procedure IsValidCurrencyEqual;
    procedure IsValidCurrencyGreat;
    procedure IsValidIntegerLittle;
    procedure IsValidIntegerEqual;
    procedure IsValidIntegerGreat;
  end;

implementation

uses
  System.SysUtils,
  Framework.Libraries.Validation.ResourceStrings;



procedure TestTMinValue.SetUp;
begin
  FMinValue := TMinValueImpl.Create(MIN_VALUE);
end;



procedure TestTMinValue.TearDown;
begin
  FMinValue.Free;
  FMinValue := nil;
end;



procedure TestTMinValue.GetErrorMessage;
begin
  CheckTrue(FMinValue.GetErrorMessage = Format(TResourceStringsValidator.RSValidation_MinValue,
    ['%s', MIN_VALUE.ToString]), 'Erro ao formatar mensagem');
end;



procedure TestTMinValue.IsValidCurrencyEqual;
var
  AValue: TValue;
begin
  AValue := 10.0;
  CheckTrue(FMinValue.isValid(AValue), 'Erro ao validar o número ' + AValue.ToString);
end;



procedure TestTMinValue.IsValidCurrencyGreat;
var
  AValue: TValue;
begin
  AValue := 11.0;
  CheckTrue(FMinValue.isValid(AValue), 'Erro ao validar o número ' + AValue.ToString);
end;



procedure TestTMinValue.IsValidCurrencyLittle;
var
  AValue: TValue;
begin
  AValue := 9.0;
  CheckFalse(FMinValue.isValid(AValue), 'Erro ao validar o número ' + AValue.ToString);
end;



procedure TestTMinValue.IsValidIntegerEqual;
var
  AValue: TValue;
begin
  AValue := 10;
  CheckTrue(FMinValue.isValid(AValue), 'Erro ao validar o número ' + AValue.ToString);
end;



procedure TestTMinValue.IsValidIntegerGreat;
var
  AValue: TValue;
begin
  AValue := 11;
  CheckTrue(FMinValue.isValid(AValue), 'Erro ao validar o número ' + AValue.ToString);
end;



procedure TestTMinValue.IsValidIntegerLittle;
var
  AValue: TValue;
begin
  AValue := 9;
  CheckFalse(FMinValue.isValid(AValue), 'Erro ao validar o número ' + AValue.ToString);
end;



procedure TestTMinValue.IsValidString;
var
  AValue: TValue;
begin
  AValue := 'abcd';
  CheckFalse(FMinValue.isValid(AValue), 'String inválida ' + AValue.ToString);
end;

initialization

// Register any test cases with the test runner
RegisterTest(TestTMinValue.Suite);

end.
